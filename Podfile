# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target 'Mirex' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Mirex
	pod 'RealmSwift'
  pod 'PromiseKit'			
  pod 'R.swift'
  pod 'Unbox'
  pod 'SDWebImage'

  target 'MirexTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MirexUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
